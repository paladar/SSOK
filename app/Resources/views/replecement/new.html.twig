{% extends 'default/index.html.twig' %}

{% block content %}
    <div class="row">
        <h1 class="col-xs-6">Dodawanie zastępstwa</h1>
        <div class="col-xs-6"><a class="btn btn-ssok" href="{{ path('replecement_index') }}">Powrót do listy</a></div>
        <div class="col-xs-12">
            {{ form_start(form) }}
            {% form_theme form 'form/form_theme.html.twig' %}
            {{ form_widget(form) }}
        </div>
        <div class="col-xs-12">
            <input class="btn btn-ssok" type="submit" value="Dodaj" />
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
